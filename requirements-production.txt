# Production requirements for Linux deployment (Render/Supabase)
# Only includes packages actually used in the codebase

# Core Django
Django==5.2.4
djangorestframework==3.16.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.8.0
drf-yasg==1.21.10

# Database
psycopg2-binary==2.9.10
dj-database-url==2.1.0

# Environment
python-dotenv==1.1.1

# Setuptools (fixes pkg_resources issue with razorpay)
setuptools==75.6.0

# Image processing (used in PDF generation)
Pillow==11.3.0

# PDF generation (actually used in codebase)
reportlab==4.0.7

# Payment gateway
razorpay==1.4.2

# HTTP requests
requests==2.32.4

# Production server
gunicorn==21.2.0
whitenoise==6.6.0

# Core dependencies
asgiref==3.9.1
sqlparse==0.5.3
pytz==2025.2

# JWT
PyJWT==2.10.1

# YAML (for API docs)
PyYAML==6.0.2

# CORS
urllib3==2.5.0
certifi==2025.6.15